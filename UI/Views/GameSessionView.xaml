<UserControl x:Class=" NR155910155992.MemoGame.UI.Views.GameSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:NR155910155992.MemoGame.UI.ViewModels"
             xmlns:local="clr-namespace:NR155910155992.MemoGame.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=vm:GameSessionViewModel}">

    <Grid Background="#F3E5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <HeaderedContentControl Grid.Row="0" 
                        Style="{StaticResource NavBarStyle}"
                        Header="Game History">
        </HeaderedContentControl>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding GameSessions}"
                  Style="{StaticResource PrimaryDataGrid}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  Margin="0,5,0,0">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Date" Binding="{Binding DateFormatted}" Width="2*" />

                <DataGridTextColumn Header="Duration" Binding="{Binding DurationFormatted}" Width="*" />
                <DataGridTextColumn Header="Type" Binding="{Binding GameType}" Width="*" />
                <DataGridTextColumn Header="Mode" Binding="{Binding GameMode}" Width="*" />
                <DataGridTextColumn Header="Players" Binding="{Binding PlayerCount}" Width="*" />

                <DataGridTextColumn Header="Names" Binding="{Binding PlayerNames}" Width="3*" />

                <DataGridTextColumn Header="Winner" Binding="{Binding Winner}" Width="2*" />

                <DataGridTextColumn Header="Cards" Binding="{Binding TotalCardsUncovered}" Width="*" />
            </DataGrid.Columns>

        </DataGrid>

    </Grid>

</UserControl>

